diff --git a/src/request_router.c b/src/request_router.c
--- a/src/request_router.c
+++ b/src/request_router.c
@@ -39,11 +39,15 @@
 void release_request_session(RequestContext *ctx) {
     if (!ctx || !ctx->session) {
         return;
     }
 
     destroy_session(ctx->session);
+    ctx->session = NULL;
+    ctx->session_id = -1;
 }
 
 int handle_admin_export(const RequestContext *ctx, char *out, size_t out_size) {
+    Session *session;
     int written;
 
     if (!ctx || !out || out_size == 0) {
         return -1;
@@ -51,15 +55,20 @@
-    if (!ctx->session) {
+    if (ctx->session_id < 0) {
+        return -1;
+    }
+
+    session = find_session(ctx->session_id);
+    if (!session) {
         return -1;
     }
 
     if (strncmp(ctx->route, "/admin", 6) != 0) {
         return -1;
     }
 
-    if (strcmp(ctx->session->role, "admin") != 0) {
+    if (strcmp(session->role, "admin") != 0) {
         return -1;
     }
 
-    written = snprintf(out, out_size, "export:%s:%s", ctx->session->user, ctx->route);
+    written = snprintf(out, out_size, "export:%s:%s", session->user, ctx->route);
     return (written < 0 || (size_t)written >= out_size) ? -1 : 0;
 }
diff --git a/src/audit_log.c b/src/audit_log.c
--- a/src/audit_log.c
+++ b/src/audit_log.c
@@ -2,19 +2,25 @@
 
 #include <stdio.h>
 
 int append_request_audit(const RequestContext *ctx, char *out, size_t out_size) {
+    Session *session;
     int written;
 
     if (!ctx || !out || out_size == 0) {
         return -1;
     }
 
-    if (!ctx->session) {
+    if (ctx->session_id < 0) {
+        return -1;
+    }
+
+    session = find_session(ctx->session_id);
+    if (!session) {
         return -1;
     }
 
     written = snprintf(
         out,
         out_size,
         "audit:%d:%s:%s",
         ctx->request_id,
-        ctx->session->user,
+        session->user,
         ctx->route
     );
     return (written < 0 || (size_t)written >= out_size) ? -1 : 0;
 }
