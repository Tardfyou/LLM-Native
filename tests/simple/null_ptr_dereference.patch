diff --git a/tests/simple/null_ptr_dereference.cpp b/tests/simple/null_ptr_dereference.cpp
index 1111111..2222222 100644
--- a/tests/simple/null_ptr_dereference.cpp
+++ b/tests/simple/null_ptr_dereference.cpp
@@ -29,8 +29,13 @@ User* createUser(int id, const std::string& name, int age) {

 // Function that processes user - VULNERABLE CODE
 void processUser(User* user) {
-    // BUG: No null check before dereferencing
-    // This will crash if user is nullptr
+    // FIX: Add null check before dereferencing
+    if (user == nullptr) {
+        std::cerr << "Error: Cannot process null user pointer" << std::endl;
+        return;
+    }
     std::cout << "Processing user: " << user->name << std::endl;
     user->display();
 }
@@ -50,8 +55,13 @@ User* findUserById(int userId, User* users[], int size) {

 // Function that updates user - VULNERABLE CODE
 void updateUserAge(User* user, int newAge) {
-    // BUG: No null check before dereferencing
+    // FIX: Add null check before dereferencing
+    if (user == nullptr) {
+        std::cerr << "Error: Cannot update null user pointer" << std::endl;
+        return;
+    }
     user->age = newAge;
     std::cout << "User age updated to: " << user->age << std::endl;
 }
